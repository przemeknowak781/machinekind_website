---
import Base from '../layouts/Base.astro';
import HandsScene from '../components/HandsScene.astro';
import HeroHands from '../components/HeroHands.astro';
import HandsBackdrop from '../components/HandsBackdrop.astro';
import TeamGrid from '../components/TeamGrid.astro';
import { Image } from 'astro:assets';
import { machinekind, pwr, machinekindCount, pwrCount } from '../data/team';
import { asset } from '../lib/paths';
import korpus from '../assets/korpus.webp';
import naped from '../assets/naped-bezposredni.webp';

// Strona projektu Wojtek. Docelowo podstrona /wojtek, na razie osobny serwis.
const WOJTEK_URL = 'https://machinekind.ai';

// Takt maszyny — trzy pętle, każda o rząd wielkości wolniejsza od poprzedniej.
// To jest właściwa treść „czterech warstw": im wyżej, tym rzadziej i tym
// bardziej o zamiarze niż o momencie na wale.
const clock = [
  { hz: '400 Hz', what: 'pętla momentu' },
  { hz: '50 Hz', what: 'sieć lokomocyjna' },
  { hz: '1 Hz', what: 'model świata' },
];

// Liczebnik po polsku: 1 osoba, 2–4 osoby, 5+ osób, z wyjątkiem nastek.
function osoby(n: number): string {
  const last = n % 10;
  const teen = n % 100 >= 12 && n % 100 <= 14;
  if (n === 1) return 'osoba';
  if (!teen && last >= 2 && last <= 4) return 'osoby';
  return 'osób';
}

const domains = [
  {
    label: 'Locomotion AI',
    title: 'Chód uczony w symulacji.',
    body:
      'Sieci sterujące chodem trenujemy w symulatorze pełnej dynamiki, a te same trajektorie przenosimy na maszynę.',
    meta: '300 mln kroków · sim-to-real',
  },
  {
    label: 'World Models & VLM',
    title: 'Rozumienie przestrzeni.',
    body:
      'Modele świata i modele wizyjno-językowe dają maszynie kontekst: co widzi, gdzie może stanąć i co z tego wynika dla następnego kroku.',
    meta: 'VLM · zamiar',
  },
  {
    label: 'Robotics Engineering',
    title: 'Konstrukcja i napędy.',
    body:
      'Napędy bezpośrednie, nogi o zamkniętym łańcuchu kinematycznym, sztywność wzięta ze struktury zamiast z dodatkowej masy.',
    meta: 'direct drive · closed-loop',
  },
  {
    label: 'Design & Program',
    title: 'Prowadzenie i forma.',
    body:
      'Harmonogram, finansowanie i to, jak maszyna wygląda. Forma wynika z obciążenia: każde wycięcie odprowadza ciepło.',
    meta: 'program · design',
  },
];

// Dwa zespoły po dwóch stronach jednej maszyny. Składy i liczności idą
// z `data/team`, więc opis podziału pracy nie rozjedzie się ze składem.
const crews = [
  {
    name: 'Machinekind',
    count: machinekindCount,
    role: 'Warstwa inteligencji, inżynieria robotyki, design i prowadzenie projektu.',
    groups: machinekind.map((g) => g.label),
  },
  {
    name: 'Politechnika Wrocławska',
    count: pwrCount,
    role: 'Laboratorium Mechatroniki i Robotyki: konstrukcja, mechanika i wykonanie.',
    groups: pwr.map((g) => g.label),
  },
];

// Sygnatura rozbita na litery, żeby każda mogła mieć zablokowaną szerokość.
// Bez tego podmiana glifów przy przesypywaniu rozjeżdżałaby napis w poziomie.
const WORDMARK = 'Machinekind'.split('');

---

<Base
  title="Machinekind, kolektyw robotyki i AI z Wrocławia"
  description="Machinekind to kolektyw inżynierów uczenia maszynowego, robotyków i projektantów. Budujemy warstwę inteligencji dla maszyn działających w fizycznym świecie. Pierwsza maszyna: czworonóg W01-TEK."
>
  <!-- ============ HERO ============ -->
  <section class="hero scroll-scene">
    <div class="hero__stage">
      <HeroHands />

      <div class="wrap hero__grid">
        <div class="hero__text">
          <h1
            class="h-display hero__title"
            aria-label="Machinekind"
            data-scramble
          >
            {WORDMARK.map((ch) => <span class="ch">{ch}</span>)}
          </h1>
          <p class="lead after-hands">
            Uczymy maszyny poruszać się w świecie ludzi. Kolektyw inżynierów
            uczenia maszynowego, robotyków i projektantów. Budujemy warstwę
            inteligencji razem ze sprzętem, na którym da się ją uruchomić.
          </p>
        </div>

        <div class="hero__actions after-hands after-hands--late">
          <a class="btn" href={WOJTEK_URL}>
            Projekt Wojtek
            <svg class="btn__arrow" width="13" height="13" viewBox="0 0 13 13" fill="none" aria-hidden="true">
              <path d="M1 12 12 1M4 1h8v8" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </a>
          <a class="btn btn--ghost" href="#kontakt">Kontakt</a>
        </div>
      </div>
    </div>

    <div class="wrap">
      <p class="hero__meta mono after-hands after-hands--late">
        <span>Kolektyw, Wrocław</span>
        <span class="hero__meta-pair">
          <span>51.1079° N / 17.0385° E</span>
          <span>Est. 2025</span>
        </span>
      </p>
    </div>
  </section>

  <!-- ============ KOLEKTYW ============ -->
  <section class="section red has-backdrop" id="kolektyw">
    <HandsBackdrop hand="human" tone="paper" side="right" size="92%" top="80%" bleed="-22%" opacity={0.085} />
    <div class="wrap">
      <div class="head">
        <h2 class="h1">Punkt styku.</h2>
        <p class="lead">
          Machinekind powstał wokół jednego pytania: co musi się wydarzyć, żeby
          robot kroczący przestał być demonstracją, a stał się narzędziem.
          Odpowiedź wymaga dwóch zespołów po dwóch stronach jednej maszyny.
        </p>
      </div>

      <div class="crews">
        {
          crews.map((c, i) => (
            <div class="crews__col">
              <article class="crew">
                <p class="crew__count mono">
                  {c.count} {osoby(c.count)}
                </p>
                <h3 class="crew__name">{c.name}</h3>
                <p class="crew__role">{c.role}</p>
                <ul class="crew__groups">
                  {c.groups.map((g) => (
                    <li>{g}</li>
                  ))}
                </ul>
              </article>

              {i === 1 && (
                <p class="naming">
                  <span class="mono naming__label">Nazwa</span>
                  <span>
                    Maszyny nie są osobnym gatunkiem. Są przedłużeniem tego, co
                    ludzkie — a granica przebiega dokładnie tam, gdzie stykają
                    się dwie dłonie.
                  </span>
                </p>
              )}
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <!-- ============ DOMENY ============ -->
  <section class="section has-backdrop" id="domeny">
    <HandsBackdrop hand="robot" tone="ink" side="left" size="72%" top="18%" bleed="-30%" opacity={0.045} duration="32s" />
    <div class="wrap">
      <div class="head">
        <h2 class="h1">Cztery warstwy<br />jednej maszyny.</h2>
        <p class="lead">
          Nie dzielimy pracy na badania i wdrożenie. Każda domena kończy się
          czymś, co da się uruchomić na maszynie stojącej w laboratorium —
          i każda pracuje we własnym takcie.
        </p>
      </div>

      <ol class="clock">
        {
          clock.map((c) => (
            <li class="tick">
              <span class="tick__hz">{c.hz}</span>
              <span class="tick__what">{c.what}</span>
            </li>
          ))
        }
      </ol>

      <div class="domains">
        {
          domains.map((d, i) => (
            <article class="domain">
              <p class="domain__label">
                <span class="domain__index mono">{String(i + 1).padStart(2, '0')}</span>
                {d.label}
              </p>
              <h3 class="h3 domain__title">{d.title}</h3>
              <p class="body domain__body">{d.body}</p>
              <p class="domain__meta mono">{d.meta}</p>
            </article>
          ))
        }
      </div>
    </div>
  </section>

  <!-- ============ PROJEKTY ============ -->
  <section class="section section--tight" id="projekty">
    <div class="wrap">
      <div class="head">
        <h2 class="h1">Co stoi<br />w laboratorium.</h2>
        <p class="lead">
          Pierwsza maszyna platformy chodzi we Wrocławiu. Kolejne wersje
          powstają na tej samej bazie.
        </p>
      </div>

      <a class="project" href={WOJTEK_URL}>
        <div class="project__media">
          <video
            src={asset('media/render-korpus.mp4')}
            poster={korpus.src}
            muted
            loop
            playsinline
            preload="none"
            aria-hidden="true"
            data-clip></video>
        </div>
        <div class="project__body">
          <p class="project__tag">Projekt 01 · aktywny</p>
          <h3 class="h2 project__title">Wojtek<span class="project__code">W01-TEK</span></h3>
          <p class="body">
            Czworonóg zaprojektowany i zbudowany we Wrocławiu, na Politechnice
            Wrocławskiej. Od szkicu do prototypu w rok, pierwszy krok w 2025,
            pełne sterowanie AI od 2026. Imię odziedziczył po niedźwiedziu,
            który dźwigał to, czego nie mógł unieść nikt inny.
          </p>
          <ul class="project__facts">
            <li><span>Typ</span>robot kroczący</li>
            <li><span>Budowa</span>12 miesięcy</li>
            <li><span>Napęd</span>bezpośredni</li>
          </ul>
          <span class="project__go">
            Zobacz projekt
            <svg width="13" height="13" viewBox="0 0 13 13" fill="none" aria-hidden="true">
              <path d="M1 12 12 1M4 1h8v8" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </span>
        </div>
      </a>
    </div>
  </section>

  <!-- ============ ZESPÓŁ ============ -->
  <section class="section dark has-backdrop" id="zespol">
    <HandsBackdrop hand="robot" tone="paper" side="right" size="64%" top="9%" bleed="-26%" opacity={0.055} duration="30s" />
    <div class="wrap">
      <div class="head">
        <h2 class="h1">Kto buduje maszynę.</h2>
        <p class="lead">
          Dwanaście osób w dwóch zespołach. Po stronie uczelni konstrukcja,
          mechanika i wykonanie. Po stronie Machinekind warstwa AI, inżynieria
          robotyki, design i prowadzenie projektu.
        </p>
      </div>

      <h3 class="team-head">
        Machinekind <span class="mono">{machinekindCount} osób</span>
      </h3>
      <TeamGrid groups={machinekind} tone="dark" />

      <h3 class="team-head team-head--second">
        Politechnika Wrocławska, Laboratorium Mechatroniki i Robotyki
        <span class="mono">{pwrCount} osoby</span>
      </h3>
      <TeamGrid groups={pwr} tone="dark" />
    </div>
  </section>

  <!-- ============ ZAPLECZE ============ -->
  <section class="section section--tight" id="partner">
    <div class="wrap partner">
      <div class="partner__text">
        <h2 class="h2">Politechnika Wrocławska.</h2>
        <p class="body">
          Maszyna powstała w Laboratorium Mechatroniki i Robotyki Katedry
          Podstaw Konstrukcji Maszyn i Układów Mechatronicznych, z finansowaniem
          z projektu zespołu prof. Radosława Zimroza. Czworonóg jest
          rozszerzeniem prowadzonej tam linii robotów mobilnych i inspekcyjnych.
        </p>
        <dl class="partner__facts">
          <div><dt>LMiR</dt><dd>miejsce budowy</dd></div>
          <div><dt>KPKMiUM</dt><dd>katedra</dd></div>
          <div><dt>AMICOS</dt><dd>projekt</dd></div>
        </dl>
      </div>
      <figure class="partner__media">
        <Image src={naped} alt="Napęd bezpośredni maszyny W01-TEK" widths={[520, 1040]} sizes="(min-width: 900px) 520px, 100vw" loading="lazy" />
        <figcaption>Napęd bezpośredni: silnik sprzężony wprost z przegubem</figcaption>
      </figure>
    </div>
  </section>

  <!-- ============ KONTAKT ============ -->
  <section class="section red contact" id="kontakt">
    <div class="wrap">
      <h2 class="h-display contact__title">Zbudujmy<br />następny.</h2>
      <div class="contact__row">
        <p class="lead">
          Prototyp chodzi we Wrocławiu. Zapraszamy inwestorów, partnerów
          przemysłowych i media na pokaz.
        </p>
        <a class="btn" href="mailto:hello@wo1.tech">
          Umów pokaz
          <svg class="btn__arrow" width="13" height="13" viewBox="0 0 13 13" fill="none" aria-hidden="true">
            <path d="M1 12 12 1M4 1h8v8" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </a>
      </div>
      <HandsScene variant="paper" class="contact__scene" />
    </div>
  </section>
</Base>

<script>
  // Sygnatura przesypuje litery, zanim ułoży się w słowo. Litery losowane są
  // wyłącznie ze zbioru liter samego słowa, więc szerokości zostają w tym
  // samym zakresie, a napis nie zamienia się w przypadkowy szum.
  //
  // Bez skryptu w nagłówku stoi po prostu gotowe słowo — efekt jest dodatkiem,
  // nie warunkiem czytelności. Nazwa dostępnościowa siedzi w aria-label, więc
  // czytnik ekranu nigdy nie przeczyta przesypywanych liter.
  const wordmark = document.querySelector<HTMLElement>('[data-scramble]');
  const calm = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

  if (wordmark && !calm) {
    const cells = [...wordmark.querySelectorAll<HTMLElement>('.ch')];
    const target = cells.map((c) => c.textContent ?? '');
    const pool = [...new Set(target.map((c) => c.toUpperCase()))];

    const STEP = 132; // co tyle zatrzaskuje się kolejna litera: jedenaście
    // liter składa się w półtorej sekundy, czyli do chwili zetknięcia opuszków
    const TICK = 45; // co tyle podmienia się glif

    // Przesypywanie rusza w pierwszej klatce, razem z dłońmi, a nie po
    // opóźnieniu — słowo formuje się przez cały ich lot. Punktem zerowym jest
    // pierwsza klatka, w której kroje są już wczytane: dopiero wtedy można
    // zablokować szerokości kratek, a bez blokady napis skakałby w poziomie.
    // Do tego czasu litery już się przesypują, więc gotowe słowo nie mignie.
    let origin = 0;
    let lastTick = 0;

    const frame = (now: number) => {
      if (!origin) {
        if (document.fonts.status !== 'loaded') {
          // Kroju jeszcze nie ma, więc szerokości mierzyć nie ma z czego.
          // Litery już się przesypują — napis stoi w kratkach zwykłego tekstu
          // i może drgnąć, ale nie mignie gotowym słowem.
          cells.forEach((c) => {
            c.textContent = pool[(Math.random() * pool.length) | 0];
          });
          requestAnimationFrame(frame);
          return;
        }
        // Szerokość kratki bierze się z docelowej litery, nie z losowej.
        cells.forEach((c, i) => {
          c.textContent = target[i];
          c.style.width = `${c.getBoundingClientRect().width}px`;
        });
        origin = now;
      }

      const locked = Math.floor((now - origin) / STEP);
      if (locked >= cells.length) {
        cells.forEach((c, i) => (c.textContent = target[i]));
        return;
      }

      if (now - lastTick >= TICK) {
        lastTick = now;
        cells.forEach((c, i) => {
          if (i < locked) {
            if (c.textContent !== target[i]) {
              c.textContent = target[i];
              c.classList.add('ch--lock');
            }
          } else {
            c.textContent = pool[(Math.random() * pool.length) | 0];
          }
        });
      }
      requestAnimationFrame(frame);
    };
    requestAnimationFrame(frame);
  }

  // Film w karcie projektu rusza dopiero w kadrze i tylko wtedy, gdy widz
  // nie prosił o ograniczenie ruchu. Do tego czasu leży plakat, więc bez
  // skryptu strona nadal jest kompletna.
  const clip = document.querySelector<HTMLVideoElement>('video[data-clip]');
  const still = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

  if (clip && !still && 'IntersectionObserver' in window) {
    const io = new IntersectionObserver(
      (entries) => {
        for (const e of entries) {
          if (e.isIntersecting) {
            clip.preload = 'auto';
            clip.play().catch(() => {});
          } else {
            clip.pause();
          }
        }
      },
      { threshold: 0.3 }
    );
    io.observe(clip);
  }
</script>

<style>
  /* ---------- hero ---------- */
  .hero {
    padding-bottom: clamp(2rem, 4vw, 3rem);
  }

  /* Jednostka kompozycji dłoni. Bierze mniejszą z miar okna, więc na
     szerokim i niskim ekranie scena kurczy się zamiast wyjeżdżać poza kadr. */
  .hero__stage {
    --hand-u: min(1vw, 1.55vh);
    position: relative;
    isolation: isolate;
    padding-top: clamp(2.5rem, 6vw, 5rem);
    padding-bottom: clamp(2rem, 4vw, 3.5rem);
  }

  /* Tekst i przyciski rozchodzą się na przeciwne rogi sceny, żeby
     przekątna dłoni miała przez co przejść. */
  .hero__grid {
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    gap: clamp(2.5rem, 8vw, 7rem);
    min-height: max(26rem, calc(54 * var(--hand-u)));
  }

  /* Ułożenie pionowe. Bryła ma twardy sufit w rem, bo przy 700px sama
     proporcja do szerokości robiła z niej kolos na dwa ekrany. Wysokość
     sceny liczy się z tej samej szerokości, więc nic się nie przycina. */
  @media (max-width: 779px) {
    .hero__stage {
      --hand-w: min(calc(126 * var(--hand-u)), 34rem);
    }

    .hero__grid {
      min-height: calc(var(--hand-w) / 1.63 + 21rem);
    }
  }

  .hero__text {
    max-width: 46rem;
  }

  /* Sygnatura rodzi się w punkcie styku dłoni, na środku ekranu, i dopiero
     potem jedzie na swoje miejsce w układzie.

     `--title-x` to droga od lewej krawędzi kolumny tekstu do środka okna:
     połowa okna, minus połowa własnej szerokości, minus to, o ile kolumna
     jest odsunięta od krawędzi strony. Dzięki temu wyśrodkowanie wychodzi
     samo na każdej szerokości, bez liczby wpisanej na sztywno. */
  .hero__title {
    --title-x: calc(
      50vw - 50% - max(0px, (100vw - var(--wrap)) / 2) - var(--gutter)
    );
    --title-y: -5vh;
    margin-bottom: clamp(1.25rem, 2.5vw, 2rem);
    white-space: nowrap;
    animation: title-travel 3s cubic-bezier(0.65, 0, 0.35, 1) both;
  }

  /* Napis stoi na środku od zerowej sekundy i składa się z liter w tym
     samym czasie, w którym dłonie lecą ku sobie. Rusza z miejsca dopiero
     przy zetknięciu opuszków — w 56,8% biegu, czyli tam, gdzie zaczyna się
     obrót dłoni — i dojeżdża razem z nimi. Krycia nie ma w ogóle: napis
     albo stoi w pełnym atramencie, albo jeszcze nie ma w nim liter. */
  @keyframes title-travel {
    0% {
      translate: var(--title-x) var(--title-y);
    }
    57% {
      translate: var(--title-x) var(--title-y);
    }
    100% {
      translate: 0 0;
    }
  }

  /* W układzie pionowym kolumna tekstu zajmuje całą szerokość, więc napis
     i tak stoi na środku. Zostaje samo pojawienie się, bez przejazdu. */
  @media (max-width: 779px) {
    .hero__title {
      --title-x: 0px;
      --title-y: 0px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero__title {
      animation: none;
      translate: none;
      visibility: visible;
    }
  }

  /* Litera w kratce o zablokowanej szerokości: glif może się zmieniać,
     a napis stoi w miejscu. Przed zablokowaniem szerokości kratka zachowuje
     się jak zwykły tekst, więc bez skryptu nic się nie zmienia. */
  .ch {
    display: inline-block;
    text-align: center;
  }

  .ch--lock {
    animation: ch-lock 0.3s var(--ease);
  }

  @keyframes ch-lock {
    from {
      opacity: 0.5;
      translate: 0 0.05em;
    }
    to {
      opacity: 1;
      translate: none;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .ch--lock {
      animation: none;
    }
  }

  /* Podtytuł nagłówka czyta się inaczej niż akapit w środku strony: stoi
     sam na bieli, pod sygnaturą wysoką na kilkanaście centymetrów, i to on
     mówi, czym kolektyw jest. Dlatego stopień wyżej niż zwykły `.lead`,
     atrament przygaszony zamiast szarego i interlinia ściągnięta do 1,45 —
     przy 1,5 i szarości rozłaził się na pięć luźnych wierszy. Miara 38
     znaków trzyma cztery wiersze o zbliżonej długości. */
  .hero__text .lead {
    max-width: 38ch;
    font-size: clamp(1.15rem, 0.92rem + 0.62vw, 1.5rem);
    line-height: 1.45;
    color: var(--ink-1);
    letter-spacing: -0.005em;
  }

  .hero__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
  }

  @media (min-width: 900px) {
    .hero__actions {
      align-self: flex-end;
    }
  }

  /* Ten sam podział kolumn co nagłówki sekcji niżej, żeby pasek pod
     nagłówkiem strony siadał w rytmie strony, a nie rozjeżdżał się sam. */
  .hero__meta {
    display: grid;
    gap: 0.45rem clamp(2rem, 6vw, 5rem);
    margin-top: clamp(1.5rem, 3vw, 2.25rem);
    padding-top: 1.1rem;
    border-top: 1px solid var(--line);
  }

  .hero__meta-pair {
    display: flex;
    flex-wrap: wrap;
    gap: 0.45rem 2rem;
    justify-content: space-between;
  }

  @media (min-width: 900px) {
    .hero__meta {
      grid-template-columns: minmax(0, 1.15fr) minmax(0, 1fr);
    }
  }

  /* ---------- dwa zespoły ----------
     Sekcja nazywa się „punkt styku", więc pokazuje styk: dwie kolumny
     rozdzielone jedną linią. Wcześniej stała tu ściana abstrakcji i paragon
     z czterema faktami, z których żaden nie mówił, kto co robi. */
  .crews {
    display: grid;
    gap: clamp(2rem, 4vw, 3rem);
    border-top: 1px solid var(--on-red-line);
    padding-top: clamp(1.75rem, 3vw, 2.5rem);
  }

  .crews__col {
    display: flex;
    flex-direction: column;
  }

  /* W jednej kolumnie zespoły dzieli kreska w poziomie — bez niej skład
     jednego wchodził wprost w liczebność drugiego. */
  @media (max-width: 779px) {
    .crews__col:nth-child(2) {
      padding-top: clamp(1.75rem, 4vw, 2.5rem);
      border-top: 1px solid var(--on-red-line);
    }
  }

  @media (min-width: 780px) {
    .crews {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      column-gap: clamp(2.5rem, 6vw, 5rem);
    }

    /* Linia podziału to jedyna kreska między zespołami — ona jest tym stykiem.
       Biegnie przez całą wysokość bloku, bo obie kolumny kończą się równo:
       notka o nazwie dosuwa się do dołu prawej. */
    .crews__col:nth-child(2) {
      padding-left: clamp(2.5rem, 6vw, 5rem);
      border-left: 1px solid var(--on-red-line);
    }
  }

  .crew__count {
    color: var(--on-red-2);
    font-variant-numeric: tabular-nums;
  }

  .crew__name {
    font-family: var(--display);
    font-weight: 500;
    text-transform: uppercase;
    font-size: clamp(1.7rem, 3.2vw, 2.6rem);
    line-height: 0.94;
    letter-spacing: 0.012em;
    margin-block: 0.5rem 0.85rem;
  }

  .crew__role {
    color: var(--on-red-2);
    max-width: 34ch;
  }

  .crew__groups {
    list-style: none;
    padding: 0;
    margin-top: 1.5rem;
    display: grid;
    gap: 0.55rem;
  }

  .crew__groups li {
    font-size: 0.95rem;
    padding-top: 0.55rem;
    border-top: 1px solid var(--on-red-line);
  }

  /* Skąd nazwa — myśl założycielska, ale nie pierwsze, co się czyta. */
  .naming {
    display: grid;
    gap: 0.5rem;
    margin-top: clamp(2rem, 4vw, 3rem);
    padding-top: 1.25rem;
    border-top: 1px solid var(--on-red-line);
    color: var(--on-red-2);
    max-width: 46ch;
  }

  .naming__label {
    color: var(--on-red-2);
  }

  /* Skład Machinekindu ma cztery grupy, Politechniki dwie, więc prawa
     kolumna kończy się wyżej. Notka zajmuje to miejsce zamiast zostawiać
     dziurę i ciągnąć sekcję w dół. */
  @media (min-width: 780px) {
    .naming {
      margin-top: auto;
      padding-top: clamp(2rem, 4vw, 3rem);
      max-width: 38ch;
    }
  }

  /* ---------- takt maszyny ----------
     Trzy pętle, każda o rząd wielkości wolniejsza. To jest teza sekcji, więc
     stoi nad domenami i czyta się w jednym spojrzeniu. Liczba niesie treść,
     więc idzie krojem wyświetlanym, a nie jako etykietka w kółku. */
  .clock {
    list-style: none;
    padding: 0;
    display: grid;
    gap: 0;
    border-top: 1px solid var(--line);
  }

  @media (min-width: 700px) {
    .clock {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }
  }

  .tick {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    align-items: baseline;
    gap: 0.35rem 1rem;
    padding-block: 1.1rem;
    border-bottom: 1px solid var(--line);
  }

  @media (min-width: 700px) {
    .tick {
      grid-template-columns: 1fr;
      gap: 0.3rem;
      padding-right: clamp(1rem, 3vw, 2.5rem);
    }
  }

  .tick__hz {
    font-family: var(--display);
    font-weight: 500;
    font-size: clamp(2rem, 4vw, 3.4rem);
    line-height: 0.9;
    letter-spacing: 0.01em;
    font-variant-numeric: tabular-nums;
  }

  .tick__what {
    font-size: 1rem;
    color: var(--ink-1);
  }

  /* ---------- domeny ----------
     Cztery wiersze przez całą szerokość ciągnęły sekcję na półtora tysiąca
     pikseli i zostawiały pustą prawą połowę, a oznaczenia lądowały tysiąc
     pikseli od zdania, którego dotyczyły. Teraz dwie kolumny po dwa pola,
     a oznaczenie stoi pod swoim akapitem. */
  /* Takt i domeny dzielą jedną siatkę kresek: dolna linia taktu jest górną
     linią pierwszego pola, więc między nimi nie ma pasa z dwiema liniami. */
  .domains {
    display: grid;
    gap: 0;
  }

  @media (min-width: 900px) {
    .domains {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      column-gap: clamp(2.5rem, 6vw, 5rem);
    }
  }

  .domain {
    padding-block: clamp(1.5rem, 3vw, 2.25rem);
    border-bottom: 1px solid var(--line);
  }

  .domain__label {
    display: flex;
    align-items: baseline;
    gap: 0.7rem;
    font-size: 0.85rem;
    font-weight: 600;
    color: var(--red);
    margin-bottom: 0.6rem;
  }

  .domain__index {
    color: var(--ink-3);
    font-weight: 400;
  }

  .domain__title {
    margin-bottom: 0.6rem;
    max-width: 22ch;
  }

  .domain__body {
    max-width: 46ch;
  }

  .domain__meta {
    margin-top: 0.9rem;
    color: var(--ink-3);
  }

  /* ---------- projekt ---------- */
  .project {
    display: grid;
    gap: clamp(1.75rem, 4vw, 3.5rem);
    border: 1px solid var(--line);
    padding: clamp(1.25rem, 2.5vw, 2rem);
    transition: border-color 0.4s var(--ease);
  }

  @media (min-width: 980px) {
    .project {
      grid-template-columns: minmax(0, 1.05fr) minmax(0, 1fr);
      align-items: center;
    }
  }

  .project:hover {
    border-color: var(--red);
  }

  .project__media {
    background: var(--ink);
    aspect-ratio: 16 / 11;
    overflow: hidden;
  }

  .project__media video {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .project__body {
    padding-right: clamp(0rem, 2vw, 2rem);
  }

  .project__tag {
    font-size: 0.85rem;
    font-weight: 600;
    color: var(--red);
    margin-bottom: 1rem;
  }

  .project__title {
    display: flex;
    flex-wrap: wrap;
    align-items: baseline;
    gap: 0.9rem;
    margin-bottom: 1rem;
  }

  .project__code {
    font-family: var(--mono);
    font-size: 0.85rem;
    letter-spacing: 0.06em;
    color: var(--ink-3);
    text-transform: none;
  }

  .project__facts {
    list-style: none;
    padding: 1.1rem 0 0;
    margin: 1.5rem 0;
    display: grid;
    gap: 0.6rem;
    border-top: 1px solid var(--line);
  }

  .project__facts li {
    display: flex;
    gap: 1rem;
    font-size: 0.95rem;
    color: var(--ink);
  }

  .project__facts span {
    color: var(--ink-3);
    min-width: 7rem;
  }

  .project__go {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    font-size: 0.92rem;
    font-weight: 500;
    color: var(--red);
  }

  .project:hover .project__go svg {
    transform: translate(3px, -3px);
  }

  .project__go svg {
    transition: transform 0.4s var(--ease);
  }

  /* ---------- zespół ---------- */
  .team-head {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 1rem;
    font-family: var(--body);
    font-size: 1rem;
    font-weight: 600;
    letter-spacing: 0;
    text-transform: none;
    color: var(--on-dark-1);
    padding-bottom: 1rem;
    margin-bottom: clamp(1.75rem, 3vw, 2.5rem);
    border-bottom: 1px solid var(--red);
  }

  .team-head span {
    color: var(--on-dark-3);
    flex: none;
  }

  .team-head--second {
    margin-top: clamp(3rem, 6vw, 5rem);
  }

  /* ---------- zaplecze ---------- */
  .partner {
    display: grid;
    gap: clamp(2rem, 5vw, 4rem);
    align-items: center;
  }

  @media (min-width: 900px) {
    .partner {
      grid-template-columns: minmax(0, 1fr) minmax(0, 0.85fr);
    }
  }

  .partner__text h2 {
    margin-bottom: 1.1rem;
  }

  .partner__facts {
    margin: 1.75rem 0 0;
    display: grid;
    gap: 0;
  }

  .partner__facts div {
    display: flex;
    gap: 1.25rem;
    border-top: 1px solid var(--line);
    padding-block: 0.65rem;
  }

  .partner__facts dt {
    font-family: var(--mono);
    font-size: 0.85rem;
    color: var(--ink);
    min-width: 6rem;
  }

  .partner__facts dd {
    margin: 0;
    font-size: 0.95rem;
    color: var(--ink-2);
  }

  .partner__media {
    margin: 0;
  }

  /* Kadr kwadratowy: przy 4/5 zdjęcie było dwa razy wyższe od kolumny
     tekstu i przechylało całą sekcję. */
  .partner__media :global(img) {
    width: 100%;
    height: auto;
    aspect-ratio: 1 / 1;
    object-fit: cover;
  }

  .partner__media figcaption {
    margin-top: 0.85rem;
    font-size: 0.85rem;
    color: var(--ink-3);
  }

  /* ---------- kontakt ---------- */
  .contact {
    overflow: hidden;
  }

  .contact__title {
    margin-bottom: clamp(2rem, 4vw, 3rem);
  }

  .contact__row {
    display: grid;
    gap: 1.75rem;
    align-items: end;
  }

  @media (min-width: 780px) {
    .contact__row {
      grid-template-columns: minmax(0, 1fr) auto;
      gap: 3rem;
    }
  }

  /* Scena mieszka w komponencie, więc sięgamy po nią spod zakresu sekcji. */
  .contact :global(.scene) {
    margin-top: clamp(3.5rem, 8vw, 7rem);
    opacity: 0.2;
  }
</style>
